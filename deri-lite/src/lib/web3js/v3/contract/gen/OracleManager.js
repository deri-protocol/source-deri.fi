// this file is generated by script, don't modify it !!!
import { ContractBase } from '../../../shared/contract/contract_base.js'
import { deleteIndexedKey } from '../../../shared/utils/web3.js'
import { oracleManagerAbi } from '../abi/oracleManagerAbi.js'

export class OracleManager extends ContractBase {
  // init
  constructor(chainId, contractAddress, opts = {}) {
    super(chainId, contractAddress, oracleManagerAbi)
    // for pool use
    this.initialBlock = opts.initialBlock || ""
  }

  // query
  async admin() {
    const res = await this._call('admin', [])
    return deleteIndexedKey(res)
  }
  async getOracleBySymbol(symbol) {
    const res = await this._call('getOracle(string)', [symbol])
    return deleteIndexedKey(res)
  }
  async getOracle(symbolId) {
    const res = await this._call('getOracle(bytes32)', [symbolId])
    return deleteIndexedKey(res)
  }
  async getValue(symbolId) {
    const res = await this._call('getValue', [symbolId])
    return deleteIndexedKey(res)
  }
  async nameId() {
    const res = await this._call('nameId', [])
    return deleteIndexedKey(res)
  }
  // async value(symbolId) {
  //   const res = await this._call('value', [symbolId])
  //   return deleteIndexedKey(res)
  // }
  async versionId() {
    const res = await this._call('versionId', [])
    return deleteIndexedKey(res)
  }

  // tx
  // async delOracle(accountAddress, symbolId) {
  //   return await this._transact('delOracle', [symbolId], accountAddress)
  // }
  // async delOracle(accountAddress, symbol) {
  //   return await this._transact('delOracle', [symbol], accountAddress)
  // }
  // async setAdmin(accountAddress, newAdmin) {
  //   return await this._transact('setAdmin', [newAdmin], accountAddress)
  // }
  // async setOracle(accountAddress, oracleAddress) {
  //   return await this._transact('setOracle', [oracleAddress], accountAddress)
  // }
  // async updateValue(accountAddress, symbolId, timestamp_, value_, v_, r_, s_) {
  //   return await this._transact('updateValue', [symbolId, timestamp_, value_, v_, r_, s_], accountAddress)
  // }

}