// this file is generated by script, don't modify it !!!
import { ContractBase } from '../../../shared/contract/contract_base.js'
import { deleteIndexedKey } from '../../../shared/utils/web3.js'
import { rewardVaultAbi } from '../abi/rewardVaultAbi.js'

export class RewardVault extends ContractBase {
  // init
  constructor(chainId, contractAddress, opts = {}) {
    super(chainId, contractAddress, rewardVaultAbi)
    // for pool use
    this.initialBlock = opts.initialBlock || ""
  }

  // query
  async pendingByAddress(address_1) {
    const res = await this._call('pending(address)', [address_1])
    return deleteIndexedKey(res)
  }
  async pending(uint256_1) {
    const res = await this._call('pending(uint256)', [uint256_1])
    return deleteIndexedKey(res)
  }
  async rewardPerSecond() {
    const res = await this._call('rewardPerSecond', [])
    return deleteIndexedKey(res)
  }

  // tx
  async claim(accountAddress, opts) {
    return await this._transact('claim', [], accountAddress, opts)
  }
  // async updateVault(accountAddress, , , ) {
  //   return await this._transact('updateVault', [, , ], accountAddress)
  // }

}