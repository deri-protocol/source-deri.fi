// this file is generated by script, don't modify it !!!
import { ContractBase } from '../../../shared/contract/contract_base.js'
import { tokenVaultAbi } from '../abi/tokenVaultAbi.js'

export class TokenVault extends ContractBase {
  // init
  constructor(chainId, contractAddress, opts = {}) {
    super(chainId, contractAddress, tokenVaultAbi)
    // for pool use
    this.initialBlock = opts.initialBlock || ""
  }

  // query


  // tx
  async withdraw(accountAddress, token, amount, opts) {
    return await this._transact('withdraw', [token, amount], accountAddress, opts)
  }

}