// import { getSymbolsOracleInfo } from "../utils/oracle"
// import { TIMEOUT } from "./shared"

// describe('oracle', () => {
//   it('getSymbolsOracleInfo', async () => {
//     const res = await getSymbolsOracleInfo('97', ['SOLUSDT', 'ETH^2', 'ETHUSD-3000-C'])
//     expect(res).toEqual([])
//   }, TIMEOUT)
// })